# Port
server:
  port: 8083

# App
spring:
  application:
    name: NBU-SERVICE
  cloud:
    config:
      enabled: false

# Eureka client
eureka:
  instance:
    hostname: eureka-client
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://${CONTAINER_NAME}:8761/eureka

