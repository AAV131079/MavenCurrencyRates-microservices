server:
  port: 8282

spring:
  jpa:
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: none
    generate-ddl: true
    show-sql: true
    properties:
      hibernate.jdbc.lob.non_contextual_creation: true
  datasource:
    url: jdbc:postgresql://localhost:5432/currencyratesdb
    username: aav
    password: 13101979
    driver-class-name: org.postgresql.Driver
    hikari:
      schema: app
  flyway:
    enabled: true
    schemas: app
    validate-on-migrate: true
    locations: classpath:db/migration